type LikeUser {
  _id: ID!
  username: String!
  image: String
  createdAt: String!
}

type LikesOfPost {
  count: Int!
  users: [LikeUser!]
}

type Query {
  getLikesForPost(postId: ID!): LikesOfPost!
}
