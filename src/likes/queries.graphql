type Like {
  _id: ID!
  postId: ID!
  userId: ID!
  createdAt: String!
  updatedAt: String!
}

type LikeResponse {
  code: Int!
  success: Boolean!
  message: String!
}

type Query {
  getLikesForPost(postId: ID!): LikeResponse!
}
